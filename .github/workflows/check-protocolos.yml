# ARCHIVO ELIMINADO — funcionalidad absorbida por validate-canon.yml y auditoria-semanal.yml
# Mantenido como placeholder para evitar referencias rotas. Puede borrarse manualmente.
name: '[DEPRECATED] check-protocolos'
on:
  workflow_dispatch:
jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Este workflow está deprecated. Usa auditoria-semanal o validate-canon.'
