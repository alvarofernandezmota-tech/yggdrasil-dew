# Diario — 13 junio 2026 (sábado)

> Levantado a las 12:00. Sesión de infraestructura hasta ~13:45.

---

## Resumen

Mañana dedicada 100% a resolver el acceso remoto seguro Acer → Madre que quedó pendiente ayer. SSH con clave, fail2ban activo, whisrs instalado en Acer.

---

## Cronología

### ~12:00 — Inicio del día
- Levantado. Sin margen de tiempo perdido, directo al ordenador.

### 12:00–12:55 — SSH Acer → Madre
- Primer intento `ssh-copy-id` failó — clave no existía todavía
- Generada clave Ed25519: `ssh-keygen -t ed25519 -C "acer"`
- Copiada a Madre con `ssh-copy-id` → pidió contraseña una única vez
- `ssh varopc@100.91.112.32` → entró sin contraseña ✅
- Problema: `ssh madre "uptime"` pedía contraseña — `~/.ssh/config` no existía
- Creado `~/.ssh/config` con alias `madre` → `ssh madre "uptime"` ✅

### 12:55–13:25 — whisrs instalado en Acer
- `yay -S whisrs-git` — compilación 6m44s, 114 tests OK
- Backend: whisper.cpp local, modelo `base.en` (142MB)
- Idioma: Español, filler removal, audio feedback, overlay carbon
- Keybinding `Super+W` en Hyprland, servicio systemd activo

### 13:24–13:44 — Fail2ban en Madre
- Instalado vía `ssh -t madre "sudo pacman -S fail2ban"`
- Creado `jail.local`: 5 intentos / 10min / ban 1h
- Verificado: `sudo fail2ban-client status sshd` → Currently banned: 0 ✅

---

## Problemas encontrados

| Problema | Causa | Solución |
|---|---|---|
| `ssh-copy-id: No such file` | Clave no existía aún | `ssh-keygen` primero |
| `ssh madre` → `Name not known` | `~/.ssh/config` no existía | Crearlo manualmente |
| `sudo` sin terminal remota | SSH no da TTY | `ssh -t madre "sudo ..."` |
| `jail sshd does not exist` | Faltaba `jail.local` | Crear `/etc/fail2ban/jail.local` |
| Socket error fail2ban | Timing — comando antes de arrancar | Reejecutar tras arranque |

---

## Estado final

| Servicio | Estado |
|---|---|
| sshd | ✅ Activo |
| Tailscale | ✅ `100.91.112.32` |
| `ssh madre` sin contraseña | ✅ |
| fail2ban vigilando SSH | ✅ |
| whisrs en Acer | ✅ |

## Pendiente
- [ ] UFW firewall en Madre
- [ ] `sshd_config` hardening (`PasswordAuthentication no`)
- [ ] Ollama
- [ ] Uptime Kuma
- [ ] VNC (estaba fallando ayer, pendiente)
- [ ] Prompt terminal con nombre de máquina
- [ ] whisrs en Madre (modelo small.en + GPU)
