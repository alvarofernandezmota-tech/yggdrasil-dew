# Diario — 13 junio 2026 (sábado)

> **NOTA PARA EL AGENTE**: Al actualizar este diario, revisar EXHAUSTIVAMENTE todos los commits del día con `list_commits since/until` para no dejar ninguno fuera. Los commits de madrugada (00:xx UTC) pertenecen a la sesión del día anterior si la sesión empezó antes de medianoche. No asumir — verificar siempre.

> Levantado a las 12:00. Sesión activa desde 12:00.

---

## Resumen

Mañana de infraestructura: SSH sin contraseña resuelto, whisrs instalado en Acer, fail2ban activo y verificado en Madre. Sesión de documentación y auditoría del repo. Inicio de planificación de despliegue de thdora en Madre.

---

## Cronología

### 00:25—00:29 UTC (cierre sesión del 12, madrugada)
> Commits que pertenecen a la sesión nocturna del 12 jun aunque caen en día 13 en UTC.
- `add: incidencia-acceso-madre.md` — post-mortem profesional + plan resolución
- `audit: CONTEXT.md + CHANGELOG.md + arquitectura.md` — repo 100% sincronizado
- `audit final: plan-maestro.md + diario 2026-06-13` — inicio del diario de hoy

### ~12:00 — Levantado
- Directo al ordenador.

### 13:01 CEST (11:01 UTC) — SSH sin contraseña ✅
- `update: ssh.md + tailscale.md — flujo completo Acer→Madre documentado + permisos SSH`
- Generada clave Ed25519 en Acer: `ssh-keygen -t ed25519 -C "acer"`
- Copiada a Madre con `ssh-copy-id` → pidió contraseña una única vez
- `~/.ssh/config` no existía → creado con alias `madre`
- `ssh madre "uptime"` ✅ sin contraseña

### ~13:15 CEST — whisrs en Acer ✅
- `yay -S whisrs-git` — compilación 6m44s, 114 tests OK
- Backend: whisper.cpp local, modelo `base.en` (142MB)
- Idioma: Español, filler removal, audio feedback, overlay carbon
- Keybinding `Super+W` en Hyprland, servicio systemd activo

### 13:24 CEST — fail2ban instalado en Madre
- Instalado vía `ssh -t madre "sudo pacman -S fail2ban"`
- `systemctl enable --now fail2ban` ✅

### 13:38–13:59 CEST — fail2ban: debug jail sshd
- Primer intento: `jail sshd does not exist` — faltaba `jail.local`
- Creado `/etc/fail2ban/jail.local` con configuración básica
- Error de socket tras restart — causa: timing (cliente antes que socket)
- Solución: `sleep 3 && sudo fail2ban-client status sshd`
- **fail2ban operativo** ✅ `Currently banned: 0`, journal activo

### 13:43 CEST (11:43 UTC) — Documentación
- `docs: fail2ban completado + diario 2026-06-13`

### 13:46–13:50 CEST — Auditoría diarios
- Detectado: diario del 12 tenía solo 29 commits desde 16:47 — incorrecto
- Corregido: **42 commits desde 13:03**
- Nota de auditoría exhaustiva añadida a ambos diarios

### ~14:00 CEST — Planificación thdora en Madre
- Revisada repo [thdora](https://github.com/alvarofernandezmota-tech/thdora): Bot Telegram + FastAPI + Ollama
- Ya tiene Dockerfile + docker-compose.yml ✅
- Plan: desplegar en Madre vía Docker, repo independiente, referencia en personal-v2
- Pendiente: crear `proyectos/thdora.md` con plan de despliegue

---

## Problemas encontrados

| Problema | Causa | Solución |
|---|---|---|
| `ssh-copy-id: No such file` | Clave no existía | `ssh-keygen` primero |
| `ssh madre` → `Name not known` | `~/.ssh/config` no existía | Crearlo manualmente |
| `sudo` sin TTY remota | SSH no da terminal | `ssh -t madre "sudo ..."` |
| `jail sshd does not exist` | Faltaba `jail.local` | Crear `/etc/fail2ban/jail.local` |
| Socket error fail2ban | Timing: cliente antes que socket | `sleep 3 &&` antes del comando |

---

## Estado final Madre

| Servicio | Estado |
|---|---|
| sshd | ✅ Activo |
| Tailscale | ✅ `100.91.112.32` |
| `ssh madre` sin contraseña | ✅ |
| fail2ban + jail sshd | ✅ |
| whisrs en Acer | ✅ |

## Pendiente
- [ ] UFW firewall en Madre
- [ ] `sshd_config` hardening (`PasswordAuthentication no`)
- [ ] thdora en Madre (Docker)
- [ ] Ollama en Madre
- [ ] Uptime Kuma
- [ ] whisrs en Madre (small.en + GPU)
- [ ] lan-mouse o barrier (alternativa Input Leap)
- [ ] Prompt terminal con nombre de máquina
