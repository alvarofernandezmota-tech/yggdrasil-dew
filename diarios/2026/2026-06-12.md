# Diario — 2026-06-12 (Viernes)

> **Sesión total:** ~5.5h (15:00 → 20:30 CEST)
> **Resultado del día:** De repo vacío a sistema personal completo + input-leap-git instalado y configurado
> **Estado al cerrar:** Verificación SSH en curso · Test físico pendiente al volver

---

## ⏱️ Timeline de la sesión

| Hora (CEST) | Bloque | Resultado |
|---|---|---|
| 15:00 – 15:45 | Auditoría repo `personal` (madre) | Todas las carpetas revisadas |
| 15:45 – 16:30 | Estructura `personal-v2` + agentes Google | Repo creado y estructurado |
| 16:30 – 17:15 | Tailscale Madre + Acer | IPs fijas, SSH operativo |
| 17:15 – 18:15 | Input Leap estable → diagnóstico bloqueo | Error portal Wayland identificado |
| 18:15 – 18:30 | Documentación y alineado repo | README, CONTEXT, plan-maestro |
| 18:30 – 19:15 | `yay -S input-leap-git` + config portal | Instalación completada |
| 19:15 – 19:30 | Reinicio ambos equipos + plan parque | Sistema limpio listo |
| 19:30 – 20:30 | Verificación remota SSH + cierre sesión | Alineado al 100% |

---

## 🖥️ Servidor — estado final del día

| Componente | Estado |
|---|---|
| Tailscale Madre + Acer | ✅ |
| SSH Madre → Acer (Tailscale) | ✅ |
| UFW Zero Trust Acer | ✅ |
| `input-leap-git` instalado | ✅ |
| `~/.config/input-leap/input-leap.conf` | ✅ |
| `~/.config/systemd/user/input-leap.service` | ✅ habilitado |
| `exec-once = xdg-desktop-portal-hyprland` en hyprland.conf | ✅ |
| Reinicio limpio ambos equipos | ✅ |
| Test físico ratón | ⏳ al volver |

### Incidencias resueltas

| Problema | Causa | Solución |
|---|---|---|
| `tailscale up` → `NeedsLogin` | Cancelado con ^C antes de auth | `sudo tailscale up --force-reauth` |
| `input-leaps` estable en bucle | `InputCapture` no disponible en portal Hyprland | Migrar a `input-leap-git` + configurar portal |
| `input-leap` (sin `s`) bucle infinito | Wrapper GUI spawneando procesos | Usar solo el binario `input-leaps` para tests |

---

## 📁 Proyectos

- `personal-v2` creado y estructurado al completo desde cero
- Sistema de agentes (Perplexity + Gemini) operativo y documentado
- Ecosistema Google documentado (Colab + NotebookLM)
- Flujo Gemini ↔ Perplexity ejecutado y validado hoy

---

## 📚 Formación — aprendido hoy

- **Tailscale en Arch** — instalación, auth, fix NeedsLogin
- **Input Leap en Wayland** — diferencia `input-leaps` (servidor) vs `input-leap` (wrapper GUI)
- **libei / InputCapture** — protocolo Wayland; versión estable no lo implementa
- **AUR git builds** — paquetes `-git` adelantan parches que el estable no tiene
- **xdg-desktop-portal-hyprland** — necesario en `exec-once` para exponer `InputCapture`
- **UFW Zero Trust** — `deny incoming` + whitelist Tailscale
- **IaC personal** — toda config bajo Git, auditable y replicable

**Concepto clave:**
> *"El paquete estable va detrás. En Wayland, los protocolos nuevos (libei, InputCapture) solo están en las ramas -git. Migrar al AUR git build es la decisión correcta, no un parche temporal."*

---

## 🧠 Personal

Día enorme. De repo vacío a infraestructura funcionando con documentación real en Git — todo en una tarde.
El sistema tiene forma: filosofía, agentes, servidor configurado, diario con timeline. La próxima sesión arranca con contexto completo.

---

## ➡️ Próximos pasos

**Al volver (test físico):**
- [ ] `ssh varo@100.91.112.32` → verificar acceso
- [ ] `systemctl --user status input-leap.service` → servidor vivo
- [ ] `busctl ... | grep InputCapture` → portal expuesto
- [ ] Mover el ratón de Madre a Acer → criterio de éxito

**Siguiente sesión:**
- [ ] Fase 5: SSH Ed25519
- [ ] Migrar `yo/`, `formacion/`, `proyectos/` desde repo madre

---

_Plan completo: `setup/servidor/plan-maestro.md`_
