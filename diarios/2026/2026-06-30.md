---
tags: [diario, sesion, docker, infra, validacion]
fecha: 2026-06-30
hora-inicio: 21:10
hora-fin: 21:20
---

# 📓 Diario — 30 jun 2026

## Resumen de sesión

Sesión corta de validación post-reboot. Docker daemon reiniciado limpio, todos los contenedores recuperados y verificados. Stack al 100%.

---

## ✅ Lo que se hizo

### Docker daemon
- Reinicio limpio del daemon (logs sin errores críticos, solo sandbox warnings esperados del estado anterior)
- `docker.service` symlink creado → `multi-user.target.wants`
- Docker v29.5.1 con containerd-snapshotter y overlayfs

### Stack 13/13 contenedores UP

| Contenedor | Estado final |
|---|---|
| ollama | ✅ healthy |
| ollama-embeddings | ✅ healthy |
| qdrant | ✅ healthy |
| thdora | ✅ healthy |
| thdora-bot | ✅ healthy |
| open-webui | ✅ up (health: starting → estable) |
| uptime-kuma | ✅ up (health: starting → estable) |
| n8n | ✅ up |
| gitea | ✅ up |
| code-server | ✅ up |
| portainer | ✅ up |
| grafana | ✅ up |
| prometheus | ✅ up |

### Validación de servicios

```bash
# Ollama — modelos disponibles
curl -s http://localhost:11434/api/tags
# → qwen2.5-coder:7b (4.7GB, Q4_K_M, ctx 32k, tools)
# → qwen2.5:3b (1.9GB, Q4_K_M, ctx 32k, tools)

# THDORA FastAPI
curl -s http://localhost:8000/health
# → {"status":"ok","service":"thdora","version":"0.12.1"}

# Open WebUI
curl -s http://localhost:3001
# → HTML splash screen SvelteKit ✅
```

### Seguridad verificada
- `PasswordAuthentication no` en sshd_config ✅
- Tailscale activo: `varpc (100.91.112.32)` · `iphone-11 (100.81.187.99)` · `varo12f (100.86.119.102)` ✅

---

## 📝 Notas técnicas

- Los sandbox warnings del log de dockerd son normales al reiniciar — son IDs del estado anterior que ya no existen
- Los contenedores se reconectaron a sus redes del stack anterior (no a las nuevas ai-net/batcueva-net/monitor-net del compose nuevo) — funcional, migración pendiente para cuando se refactorice el compose
- THDORA endpoint correcto: `/health` (no `/estado`). La API está en v0.12.1
- `open-webui` y `uptime-kuma` inician con `health: starting` — se estabilizan solos en ~2 min

---

## ⏭️ Pendiente para próxima sesión

1. Pull modelos pendientes: `llama3.1:8b` + `bge-m3` + `nomic-embed-text`
2. THDORA handlers: mapear todos los endpoints disponibles (`/docs`)
3. Fix driver RTL8188FTV — AP inestable
4. SSH hardening — clave pública ambos nodos
5. Tailscale Redmi A5

---
_Sesión documentada por Perplexity vía MCP — 30 jun 2026 21:20 CEST_
