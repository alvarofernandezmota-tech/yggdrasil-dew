---
tipo: diario
author: Alvaro Fernandez Mota <alvarofernandezmota@gmail.com>
creado: 2026-07-05 21:33 CEST
actualizado: 2026-07-05 21:33 CEST
ruta: docs/diarios/2026-07-05-noche-4.md
tags: [diario, sesion, terminal, bloque-c, servicios-madre, unhealthy, errores]
status: vigente
---

# 📓 DIARIO — Sesión 05-07-2026 Noche 4

> **Inicio:** ~21:33 CEST | **Dispositivo:** iPhone (Blink SSH Madre)
> **Repos tocados:** WIKI---PERSONAL (via terminal), Madre (docker)

---

## ✅ Bloque C ejecutado en terminal — resultado

### 1. git rm archivos deprecados — INNECESARIO

```
fatal: pathspec 'wiki/estructura-ecosistema.md' did not match any files
fatal: pathspec 'wiki/herramientas-ecosistema.md' did not match any files
fatal: pathspec 'wiki/plan-seguridad.md' did not match any files
```

**¿Por qué?** Los archivos ya fueron sobrescritos por el commit e98353 via MCP.
El MCP los convirtió en redirects (no los eliminó, los reescribió).
El clon local de Madre no tenía esos cambios — el repo local estaba detrás del remoto.
**Estado: resuelto. No hace falta nada más.**

### 2. git push rechazado — RESUELTO

```
error: failed to push some refs
hint: Updates were rejected because the remote contains work that you do not have locally
```

**¿Por qué?** El clon local de Madre estaba desactualizado (los commits MCP fueron directos al remoto).
**Solución:**
```bash
cd ~/WIKI---PERSONAL
git pull --rebase origin main
```

### 3. Issues WIKI — estado real

```
#6  📓 DIARY — Sesión 28-06-2026   — about 7 days ago
#4  📓 DIARY — Sesión 22-06-2026   — about 13 days ago
#2  🗺️ ROADMAP — Ecosistema infra  — about 13 days ago
```

**Acción pendiente:** Estos 3 issues son diarios y roadmap que deben estar en Dew, no en WIKI.
Comandos correctos (ejecutar cuando hagas pull primero):
```bash
gh issue transfer 6 alvarofernandezmota-tech/yggdrasil-dew
gh issue transfer 4 alvarofernandezmota-tech/yggdrasil-dew
gh issue transfer 2 alvarofernandezmota-tech/yggdrasil-dew
```

### 4. repos privados — FALTÓ FLAG

```
use of --visibility flag requires --accept-visibility-change-consequences flag
```

**Comandos correctos:**
```bash
gh repo edit alvarofernandezmota-tech/WIKI---PERSONAL --visibility private --accept-visibility-change-consequences
gh repo edit alvarofernandezmota-tech/VIDAPERSONAL --visibility private --accept-visibility-change-consequences
```

---

## ⚠️ Estado real de servicios Madre — hallazgos críticos

### Servicios unhealthy confirmados

| Servicio | Estado | Intentos | Crítico |
|---|---|---|---|
| `tailscale_monitor` | unhealthy | #774 reintentos | ⚠️ Sí |
| `log_guardian_bot` | unhealthy | #773 reintentos | ⚠️ Sí |
| `yggdrasil_watchdog` | unhealthy | activo | ⚠️ Sí |

### Servicios healthy confirmados

| Servicio | Estado |
|---|---|
| `guardian_bot` | ✅ running / healthy |
| `network_radar` | ✅ running / healthy |
| `radar_backup` | ✅ running / none |
| `local_tripwire` | ⚠️ running / arrancando... |
| `yggdrasil-mcp` | 🟡 created / none (no arrancado) |

### Análisis de los unhealthy

**`tailscale_monitor`** — 774 reinicios en ~3 días = ~1 reinicio cada 6 minutos.
Problema de configuración o dependencia. No es un fallo puntual, es estructural.
Logs necesarios: `docker logs tailscale_monitor --tail 50`

**`log_guardian_bot`** — 773 reinicios. Mismo patrón. Posiblemente:
- Problema con token o variable de entorno
- Dependencia de servicio que no está up
- Bug en el código del bot

**`yggdrasil_watchdog`** — unhealthy desde hace 3 días según `docker ps -a`.
Este es el watchdog del ecosistema. Que esté unhealthy significa que el sistema de autovigilancia está ciego.

**`yggdrasil-mcp`** — estado `created / none` = contenedor creado pero NUNCA arrancado.
Esto explica por qué el MCP server propio no está operativo todavía.

### Issues a abrir en Madre (Bloque C ampliado)

```bash
# Abrir issue en madre-config para cada servicio crítico
gh issue create --repo alvarofernandezmota-tech/madre-config \
  --title "🔴 tailscale_monitor: unhealthy #774 reinicios" \
  --body "El contenedor lleva 774 reinicios en ~3 días. Revisar logs: docker logs tailscale_monitor --tail 100" \
  --label bug

gh issue create --repo alvarofernandezmota-tech/madre-config \
  --title "🔴 log_guardian_bot: unhealthy #773 reinicios" \
  --body "Mismo patrón que tailscale_monitor. Posible token/env roto o dependencia faltante." \
  --label bug

gh issue create --repo alvarofernandezmota-tech/madre-config \
  --title "🔴 yggdrasil-mcp: created pero nunca arrancado" \
  --body "El servidor MCP propio está en estado 'created'. Revisar docker compose y configuración." \
  --label bug
```

---

## 📝 Acciones pendientes del Bloque C (versión corregida)

```bash
# 1. Sincronizar clon local
cd ~/WIKI---PERSONAL && git pull --rebase origin main

# 2. Transferir los 3 issues de WIKI a Dew
gh issue transfer 6 alvarofernandezmota-tech/yggdrasil-dew
gh issue transfer 4 alvarofernandezmota-tech/yggdrasil-dew
gh issue transfer 2 alvarofernandezmota-tech/yggdrasil-dew

# 3. Hacer repos privados (flag correcto)
gh repo edit alvarofernandezmota-tech/WIKI---PERSONAL --visibility private --accept-visibility-change-consequences
gh repo edit alvarofernandezmota-tech/VIDAPERSONAL --visibility private --accept-visibility-change-consequences

# 4. Investigar unhealthy
docker logs tailscale_monitor --tail 100
docker logs log_guardian_bot --tail 100
docker inspect tailscale_monitor | grep -A5 Health

# 5. Arrancar yggdrasil-mcp (si la config está lista)
docker start yggdrasil-mcp
# o si usa compose:
cd ~/yggdrasil-mcp && docker compose up -d
```

---

## 📋 Estado al cierre

- **Bloque C:** Parcialmente ejecutado. Pendiente: pull + transfers + privacidad + unhealthy
- **Hallazgo nuevo:** `yggdrasil-mcp` nunca ha arrancado — el MCP server propio está inactivo
- **Hallazgo nuevo:** `tailscale_monitor` y `log_guardian_bot` con >770 reinicios = bug estructural
- **Siguiente (Copilot):** Bloques D y E
- **Siguiente (terminal):** Comandos corregidos del Bloque C

---

_Diario escrito por Perplexity MCP — 2026-07-05 21:33 CEST_
