---
fecha: 2026-07-23
sesion_inicio: 06:30 CEST
sesion_fin: en curso
tipo: Larga
repos_tocados: [yggdrasil-dew, THDORA-PERSONAL/Projects-thdora, VIDAPERSONAL]
issues_resueltos: [#74]
issues_creados: []
adrs_nuevos: ninguno
---

# Diario 2026-07-23

## Qué se hizo

- Auditoría de duplicados en Madre: `diff -rq` confirmó copias exactas entre
  `~/Obsidian/cerebro/personal/01_traking_diario` y `~/repos/VIDAPERSONAL/01_traking_diario`,
  y entre `~/Obsidian/cerebro/personal/02_formacion` y dos copias de `yggdrasil-formacion`.
  Eliminadas las carpetas duplicadas: `~/Projects/personal`, `~/repos/VIDAPERSONAL`,
  `~/ygg/yggdrasil-formacion`, `~/yggdrasil-formacion-`.
- Revisión de inventario Docker en Madre: 19 contenedores corriendo, 44 imágenes (muchas
  sin uso), 26 volúmenes, 15 `docker-compose.yml` sueltos sin compose maestro real.
- **#74 (HAL-008) — RESUELTO de verdad.** Se detectó que la sesión del 13-jul había marcado
  el token de Telegram como "rotado" en `ESTADO-SISTEMA.md`, pero el `.env` real en
  `~/Projects/thdora/.env` seguía con el token viejo `8591160057:...`, causando crash-loop
  de `thdora-bot` durante 9+ días sin que nadie lo detectara. Causa raíz confirmada: existen
  dos carpetas THDORA distintas (`THDORA-PERSONAL/` y `Projects/thdora/`), y el `.env` que
  lee el contenedor real está en la segunda. Se generó token nuevo, se editó el `.env`
  correcto, y se recreó el contenedor con `docker compose up -d --force-recreate --no-deps bot`.
  Confirmado `Up ... (healthy)`.
- Verificado que Ollama (:11434) NO está expuesto a internet — descartado del backlog de
  riesgos Tier 2 de `ESTADO-SISTEMA.md` (13-jul).
- Parados 8 contenedores no esenciales para reducir carga de Madre mientras se reestructura:
  `thdora`, `grafana`, `prometheus`, `n8n`, `gitea`, `code-server`, `kali-pentest`, `spiderfoot`.
  Se mantienen activos: `guardian_bot`, `yggdrasil_watchdog`, `network_radar`,
  `log_guardian_bot`, `tailscale_monitor`, `local_tripwire`, `uptime-kuma`, `portainer`,
  `yggdrasil-mcp`, `thdora-bot`, `radar_backup`.

## Decisiones tomadas

- Confirmado que la reestructuración del ecosistema debe seguir el plan maestro ya existente
  en `yggdrasil-dew` (ADRs, fases F0-F7, auditorías de islas) en vez de reinventar arquitectura
  nueva — el canon ya define el "tridente" dew/wiki/vida y la convención de islas.
- Detectada inconsistencia estructural real: proyectos con más de una carpeta física
  representando "el mismo" servicio (THDORA-PERSONAL vs Projects/thdora) es la causa raíz
  recurrente de bugs de configuración silenciosos. Pendiente de tratar como hallazgo formal
  en la reestructuración F2/F7.

## Bloqueos encontrados

- **`yggdrasil-mcp` en crash-loop** (`Restarting (0)`, sin healthcheck definido, `docker logs`
  vacío — el proceso muere sin loguear nada). No se ha localizado aún el `docker-compose.yml`
  real de este servicio en el filesystem de Madre. Relacionado con issue #75 (cambio de puerto
  3000→3001) pero el puerto ya está en 3001 según `docker ps`, así que el runbook de #75 no
  explica el crash-loop actual — requiere diagnóstico adicional (inspección de mounts,
  búsqueda del compose en rutas no estándar).
- Conflicto de puerto detectado: `open-webui` (definido en `~/docker-compose.yml`) intenta
  bindear el puerto 3001, que ya está ocupado por `yggdrasil-mcp`. Pendiente decidir puerto
  alternativo para `open-webui` o dejarlo parado si no es prioritario ahora mismo.

## Próxima sesión

- [ ] Issue #75 — completar diagnóstico real de `yggdrasil-mcp` (inspeccionar mounts,
      localizar compose real, revisar por qué termina sin loguear nada pese a ExitCode 0)
- [ ] Resolver conflicto de puerto 3001 entre `yggdrasil-mcp` y `open-webui`
- [ ] Issue #43 — versionar los 15 `docker-compose.yml` sueltos en `madre-config` (F7 GitOps)
- [ ] Issue #15 — cerrar puerto 21 FTP en router (pendiente, sin tocar hoy)
- [ ] Verificar puerto 22 SSH público (pendiente, sin tocar hoy)
- [ ] Fusionar/decidir destino único de THDORA-PERSONAL vs Projects/thdora (evitar duplicidad
      de `.env` que causó el incidente de hoy)

## Estado al cerrar

> Todos los repos tocados tienen git push hecho: No aplica — los cambios de hoy fueron
> operativos sobre Madre (filesystem y Docker), no sobre archivos de repos git salvo este
> diario y la actualización de `ESTADO-SISTEMA.md`, que sí se pushean directamente vía API.
