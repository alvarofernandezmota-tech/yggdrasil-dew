---
tipo: diario
author: Alvaro Fernandez Mota <alvarofernandezmota@gmail.com>
creado: 2026-07-09 15:00 CEST
actualizado: 2026-07-09 15:08 CEST
ruta: docs/diarios/2026-07-09.md
tags: [diario, sesion, seguridad, incidente, thdora, inventario, madre, infra]
status: abierto
---

# Diario — 2026-07-09

> Sesión de trabajo desde el Acer con acceso SSH a Madre.
> Agente: Perplexity MCP. Norma de sesión: documentar lo real, no lo supuesto.

---

## 🔴 Incidente de seguridad — Exposición de secretos en chat

**Hora aproximada:** 15:00 CEST

Durante la sesión de rotación del token de Telegram de THDORA, se ejecutó `cat -A .env | head -10` y el output completo del `.env` fue pegado en el chat de Perplexity, exponiendo:

| Credencial | Acción tomada |
|---|---|
| `TELEGRAM_BOT_TOKEN` | Revocado en BotFather |
| `GROQ_API_KEY` | Revocada en panel Groq |
| `GITHUB_TOKEN` (ghp_...) | Revocado en GitHub Settings → Tokens |
| `ADMIN_TOKEN` | Rotado manualmente |

**Causa raíz:** El `.env` completo fue visible porque el comando se ejecutó sin filtrar el output. El archivo no tenía el bug de espacios que se sospechaba — las claves estaban bien formadas.

**Lección aprendida:** Antes de pegar output de terminal en un chat externo, revisar visualmente que no contenga secretos. Añadir `grep -v TOKEN | grep -v KEY | grep -v SECRET` como filtro de seguridad al revisar `.env`.

> Pendiente: abrir HAL nuevo para este incidente (tercera exposición de secretos THDORA).

---

## ✅ Localización real de THDORA en Madre

Problema: `cd ~/repos/THDORA-PERSONAL` fallaba con `zoxide: no match found`.

Investigación con `docker inspect thdora --format '{{.Config.Labels}}'` reveló:

- **Ruta física real:** `/home/varopc/Projects/thdora`
- **GitHub remote:** `git@github.com:alvarofernandezmota-tech/thdora-personal.git`
- **Compose file:** `/home/varopc/Projects/thdora/docker-compose.yml`
- **Compose project:** `thdora`
- **Contenedores:** `thdora` (healthy), `thdora-bot` (healthy)

Acciones tomadas:
- Creado symlink: `ln -s /home/varopc/Projects/thdora ~/repos/thdora`
- Limpiados archivos basura 0-bytes creados el 3 Jul (residuos de un comando mal ejecutado): `=`, `[bot`, `[bot]`, `[thdora`, `[thdora]`, `CACHED`, `exporting`, `naming`, `resolve`, `unpacking`

---

## ✅ Inventario físico de Madre documentado

Se descubrió que `~/Projects/` existe como segunda raíz de proyectos además de `~/repos/`, y que hay un `docker-compose.yml` raíz en `/home/varopc/` con servicios sin repo asignado.

**Repos confirmados en ~/repos:**
`WIKI---PERSONAL`, `dev-labs`, `formacion-tech`, `investigacion-ia`, `madre-config`, `acer-config`, `VIDAPERSONAL`, `yggdrasil-secops`

**Servicios Docker activos en Madre (09/07/2026):**
`yggdrasil_watchdog`, `tailscale_monitor`, `log_guardian_bot`, `radar_backup`, `guardian_bot`, `local_tripwire`, `network_radar`, `kali-pentest`, `spiderfoot`, `code-server`, `n8n`, `gitea`, `uptime-kuma`, `portainer`, `thdora-bot`, `thdora`, `grafana`, `prometheus`

---

## ✅ Documentación actualizada en GitHub (MCP directo)

| Artefacto | Repo | Enlace | Descripción |
|---|---|---|---|
| `docs/infra/MAPA-REPOS-MADRE.md` | WIKI---PERSONAL | [ver archivo](https://github.com/alvarofernandezmota-tech/WIKI---PERSONAL/blob/main/docs/infra/MAPA-REPOS-MADRE.md) | Inventario físico completo de repos y servicios en Madre |
| `docs/canon/ownership-matrix.md` | yggdrasil-dew | [ver archivo](https://github.com/alvarofernandezmota-tech/yggdrasil-dew/blob/main/docs/canon/ownership-matrix.md) | Añadida columna ruta física, nota THDORA, nombre repo corregido |

---

## ⚠️ Pendientes que quedan abiertos

| Pendiente | Estado | Prioridad |
|---|---|---|
| Rotar token Telegram THDORA en `.env` y recrear stack | ❌ Sin completar | 🔴 Crítico |
| `yggdrasil-secops` → privado | ❌ Sin hacer | 🔴 Crítico |
| `smartctl -a /dev/sda` en Madre (HAL-005 HDD) | ❌ Sin ejecutar | 🟠 Alto |
| `docker logs yggdrasil_watchdog` + restart | ❌ Sin ejecutar | 🟠 Alto |
| ADR-005/006 → INDICE-ADR.md | ❌ Sin indexar | 🟡 Medio |
| Abrir HAL nuevo por tercera exposición secretos THDORA | ❌ Pendiente | 🟠 Alto |
| Verificar `/home/varopc/docker-compose.yml` raíz — qué servicios define | ❌ Pendiente | 🟡 Medio |
| Script `generar-inventario-madre.sh` + cron diario | ❌ Pendiente | 🟡 Medio |

---

## Contexto de sesión

- Trabajando desde **Acer** via `ssh madre` (Tailscale)
- Madre tiene **Obsidian instalado** — posible vault para notas locales
- El diario del 2026-07-07 nunca se creó — sesión sin cerrar
- Tokens comprometidos hoy ya revocados antes de continuar

---

_Diario abierto · Perplexity MCP · 2026-07-09 15:08 CEST_
