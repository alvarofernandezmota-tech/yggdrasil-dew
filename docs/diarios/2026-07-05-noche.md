---
tipo: diario
tags: [diario, sesion, limpieza, obsidian, github, triage]
estado: archivo
created: 2026-07-05
---

# 📓 DIARIO — Sesión 05-07-2026 Noche

> **Inicio:** ~15:57 CEST | **Cierre:** ~20:10 CEST
> **Nodos:** Madre (varopc) + iPhone (Blink Shell)
> **Repos tocados:** WIKI---PERSONAL, yggdrasil-dew, madre-config, formacion-tech

---

## ✅ Completado esta sesión

### Triage y transfers de issues
- Ejecutado script de transfers desde `~/WIKI---PERSONAL`
- Issues de infraestructura/ecosistema → `yggdrasil-dew`
- Issues de Madre → `madre-config`
- Issues de formación → `formacion-tech`
- WIKI depurada: quedó con solo 3 issues históricos (#2 ROADMAP, #4 DIARY 22-jun, #6 DIARY 28-jun)
- Error detectado: transfers desde Blink Shell fallaban por `zoxide: no match found` — solución: SSH a Madre primero

### Sincronización Madre
- `git pull` falló con `fatal: refusing to merge unrelated histories` (force push previo)
- Solución: `git fetch origin && git reset --hard origin/main`
- Resultado: HEAD en `0b2a5c1` — sincronizado limpio ✅

### .gitignore creado
- Primer `.gitignore` del repo creado vía MCP
- Cubre: `.obsidian/workspace.json`, `.obsidian/cache`, `.trash/`, `.env`, `logs/`, archivos grandes, `.smart-connections/`
- Mantiene: `.obsidian/` completo (plugins/themes/settings) para sync entre dispositivos

### CONVENCIONES.md — §5 Obsidian/GitHub añadida
Nuevas reglas basadas en mejores prácticas reales:
- **§5.1** Qué va al .gitignore y por qué
- **§5.2** Frontmatter estándar `tipo/tags/estado/created` — válido Obsidian + scripts
- **§5.3** Regla crítica de links: `[[wikilinks]]` solo en `docs/`, nunca en ficheros raíz
- **§5.4** Attachments siempre a `assets/`
- **§5.5** Config exacta plugin Obsidian Git

### Limpieza estructural yggdrasil-dew
- `_archivo/diarios/gestor-estados-2026-07-04.log` → `docs/diarios/`
- `inbox/` eliminado (solo tenía `logs/.keep`)
- `.env` untrackeado (era template sin secretos reales — verificado)
- `logs/macro-noche-2026-07-03.log` untrackeado
- Issue [#16](https://github.com/alvarofernandezmota-tech/yggdrasil-dew/issues/16) cerrado ✅

---

## ⚠️ Incidencias

| Incidencia | Causa | Solución |
|---|---|---|
| `zoxide: no match found` en iPhone | Blink Shell no tiene el repo clonado localmente | SSH a Madre primero |
| `fatal: refusing to merge unrelated histories` | Force push previo desde MCP | `git reset --hard origin/main` |
| `.env` subido al repo temporalmente | Commit antes de que `.gitignore` estuviera activo | `git rm --cached .env` + push |
| Issue #30 no encontrado en dew | Al transferirse, quedó como #4 en dew | Cerrado correctamente como #4 |

---

## 📋 Estado del repo al cierre

```
yggdrasil-dew/
├── ✅ .gitignore        — NUEVO hoy
├── ✅ CONVENCIONES.md  — §5 Obsidian añadida
├── ✅ docs/
│   └── ✅ diarios/        — gestor-estados migrado
├── ❌ inbox/           — eliminado
├── ❌ _archivo/        — eliminado
└── ❌ logs/            — untrackeado
```

---

## 📋 Pendiente próxima sesión

- [ ] Issue [#4 dew](https://github.com/alvarofernandezmota-tech/yggdrasil-dew/issues/4) — aún quedan `diary/` y `diarios/` en raíz si no se borraron — verificar
- [ ] Configurar Obsidian: `Settings → Files & Links → Default location for new attachments: assets/`
- [ ] Instalar plugin Obsidian Git en el vault y configurar con los parámetros de CONVENCIONES.md §5.5
- [ ] Issue [#14 dew](https://github.com/alvarofernandezmota-tech/yggdrasil-dew/issues/14) — puerto 21 FTP en router (seguridad activa)
- [ ] Deuda técnica thdora: issues #16, #18, #12 en thdora (prereq fase 7)

---

_Diario escrito por Perplexity MCP — 2026-07-05 20:10 CEST_
